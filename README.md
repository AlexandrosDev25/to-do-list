# To-Do List App

Aplicación de lista de tareas con:
- PostgreSQL
- Python CLI
- Interfaz interactiva

## Instalación
1. Instalar dependencias: `pip install -r requirements.txt`
2. Configurar variables en `.env`
3. Ejecutar: `python src/app.py`
